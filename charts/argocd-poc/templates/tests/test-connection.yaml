apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "argocd-poc.fullname" . }}-test-connection"
  labels:
    {{- include "argocd-poc.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "argocd-poc.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
